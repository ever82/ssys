# Virtual Hosts
#
# Required modules: mod_log_config

# If you want to maintain multiple domains/hostnames on your
# machine you can setup VirtualHost containers for them. Most configurations
# use only name-based virtual hosts so the server doesn't need to worry about
# IP addresses. This is indicated by the asterisks in the directives below.
#
# Please see the documentation at 
# <URL:http://httpd.apache.org/docs/2.4/vhosts/>
# for further details before you try to setup virtual hosts.
#
# You may use the command line option '-S' to verify your virtual host
# configuration.

#
# VirtualHost example:
# Almost any Apache directive may go into a VirtualHost container.
# The first VirtualHost section is used for all requests that do not
# match a ServerName or ServerAlias in any <VirtualHost> block.
#
#NameVirtualHost *:80
<VirtualHost *:80>
    ServerAdmin webmaster@ssystest
    DocumentRoot /var/www/ssystest
    ServerName ${domain}
    RewriteEngine On
    RewriteOptions Inherit
    
    <Directory />
      #Options Indexes FollowSymLinks Includes ExecCGI
      Order Deny,Allow
      Require all granted
      RewriteEngine On
      RewriteBase /
      RewriteCond %{REQUEST_FILENAME} !-f
      RewriteCond %{REQUEST_FILENAME} !-d
      RewriteRule ^(.*)$ index.php/$1 [L,QSA]
    </Directory> 
</VirtualHost>
<VirtualHost *:80>
	  ServerAdmin webmaster@ssystest_static
	  DocumentRoot "/var/www/ssystest/static"
	  ServerName ${static_domain}
    <Directory />
      Order Deny,Allow
      Require all granted
    </Directory> 
</VirtualHost>
<VirtualHost *:80>
	  ServerAdmin webmaster@ssys
	  DocumentRoot "/var/www/ssys"
	  ServerName ${ssys_domain}
    <Directory />
      Order Deny,Allow
      Require all granted
    </Directory> 
</VirtualHost>





